apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-app-production
  labels:
    app: crm-app
    deployment-strategy: blue-green
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  tls:
  - hosts:
    - crm.example.com
    secretName: crm-app-tls
  rules:
  - host: crm.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: crm-app  # Routes to active deployment (blue or green)
            port:
              number: 80
---
# Ingress for testing blue deployment
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-app-blue-preview
  labels:
    app: crm-app
    version: blue
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - blue.crm.example.com
    secretName: crm-app-blue-tls
  rules:
  - host: blue.crm.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: crm-app-blue
            port:
              number: 80
---
# Ingress for testing green deployment
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-app-green-preview
  labels:
    app: crm-app
    version: green
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - green.crm.example.com
    secretName: crm-app-green-tls
  rules:
  - host: green.crm.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: crm-app-green
            port:
              number: 80
